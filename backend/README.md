To run the backend, make sure to use venv to install the dependencies

```python
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

The backend only contains the routes for storing the vaults which contain encrypted TOC whose strucutre is depicted in the main readme.
